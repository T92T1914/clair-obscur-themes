{
  "name": "clair-obscur-verification",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test:browser": "node --test --test-concurrency=1 tests/browser/*.test.mjs"
  },
  "devDependencies": { "playwright": "1.63.0" }
}
